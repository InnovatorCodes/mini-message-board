<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=DM+Sans:ital,opsz,wght@0,9..40,100..1000;1,9..40,100..1000&display=swap" rel="stylesheet">
    <title>Home | Mini Message Board</title>
</head>
<body>
    <%- include('header', {type: 'index'}) %>
    <div class="main">
        <div class="messages">
            <% messages.map((message,index)=>{ %>
                <% const date=message.added.getDate()+'/'+(parseInt(message.added.getMonth())+1)+'/'+message.added.getFullYear() %>
                <div class="message">
                    <div class="brief">
                        <div class="text"><%= message.text %></div>
                        <div class="user">Posted by: <%= message.user %></div>
                        <div class="added">Date: <%= date %></div>
                    </div>
                    <button class="view" data-id="<%= index %>">View</button>
                </div>
            <% }) %>
        </div>
    </div>
</body>
</html>